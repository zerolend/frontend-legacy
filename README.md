# ZeroLend Legacy Interface

This repo contains the codebase for the legacy ZeroLend UI, which allows for users to access the protocol by running the UI locally on their device.

## How to use

Install it and run:

```sh
cp .env.example .env.local
yarn
yarn dev
```
